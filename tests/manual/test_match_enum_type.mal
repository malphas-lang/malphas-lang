 

enum Option[T] {
    Some(T),
    None
}

fn main() {
    let o = Option[int]::Some(42);
    
    match o {
        Option::Some(val) => {
            // This should only work if val is int
            let x: int = val; 
            println("Type check passed");
        },
        _ => {}
    };
}

