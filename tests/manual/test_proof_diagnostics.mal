 

trait Display {
    fn to_string(self: &Self) -> string;
}

struct Point {
    x: int,
    y: int
}

// This will fail because we're trying to use a type that doesn't satisfy Display
// The error should show the proof chain
fn test() {
    // Simulate a constraint error by trying to use a generic that requires Display
    // For now, just test that the diagnostic system works with a simple error
    let x: int = "hello";
}
