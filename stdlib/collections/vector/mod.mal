pub struct slice[T]{
    data: *T,
    len: usize,
    cap: usize,
}

impl[T] slice[T]{

    pub fn len(&self) -> usize {
    return self.len;
}

pub fn cap(&self) -> usize {
    return self.cap;
}

}