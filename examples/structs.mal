 

struct Point {
    x: int,
    y: int
}

enum Shape {
    Circle(int),
    Rect(int, int)
}

const PI: int = 3;

fn consume(x: int) { println(x); }
fn consumePoint(p: Point) { println(p.x); }

fn main() {
    let x = PI;
    consume(x);
    let p = Point { x: 10, y: 20 };
    consumePoint(p);
}
