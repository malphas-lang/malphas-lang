 

fn worker(c: chan int) {
    c <- 42;
}

fn main() {
    // Test Channel::new with explicit type
    let ch = Channel[int]::new(10);
    
    spawn worker(ch);
    
    let x = <-ch;
    println(x);
    if x != 42 {
        // panic("expected 42");
    }
}
