package main;

trait Greeter {
    fn greet(name: string) -> void;
}

struct Person {
    name: string
}

impl Greeter for Person {
    fn greet(name: string) {
        // Do nothing
    }
}

fn main() {
    let p = Person { name: "Alice" };
    p.greet("Bob");
}
