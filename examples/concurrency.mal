 

type IntChan = chan int;

fn worker(c: IntChan) {
    c <- 42;
}

fn main() {
    let c = Channel[int]::new(0);
    spawn worker(c);

    select {
        case let v = <-c => {
            println(v);
        }
    }
}
